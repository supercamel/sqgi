--- a/Squirrel3/squirrel/jit/sqjit_backend_aarch64_emit.cpp
+++ b/Squirrel3/squirrel/jit/sqjit_backend_aarch64_emit.cpp
@@ -177,6 +177,67 @@
 bool sqjit_a64_emit_sdiv_reg(SQJitA64Buffer *buf, unsigned rd, unsigned rn, unsigned rm)
 {
     return sqjit_a64_emit_u32(buf, 0x9AC00C00u | (rm << 16) | (rn << 5) | rd);
+}
+
+bool sqjit_a64_emit_smulh_reg(SQJitA64Buffer *buf, unsigned rd, unsigned rn, unsigned rm)
+{
+    if(rd > 31 || rn > 31 || rm > 31) return false;
+    return sqjit_a64_emit_u32(buf, 0x9B407C00u | (rm << 16) | (rn << 5) | rd);
+}
+
+bool sqjit_a64_emit_shift_right(SQJitA64Buffer *buf, unsigned rd, unsigned rn,
+    unsigned shift, bool arithmetic)
+{
+    if(rd > 31 || rn > 31 || shift > 63) return false;
+    // ASR/LSR aliases of SBFM/UBFM with imms=63.
+    return sqjit_a64_emit_u32(buf, (arithmetic ? 0x9340FC00u : 0xD340FC00u) |
+        (shift << 16) | (rn << 5) | rd);
+}
+
+bool sqjit_a64_emit_divmod_constant(SQJitA64Buffer *buf, SQInteger divisor, bool remainder)
+{
+    if(sizeof(SQInteger) != 8 || divisor <= 0) return false;
+    if(divisor == 1) return !remainder || sqjit_a64_emit_mov_imm_x(buf, 9, 0);
+    const uint64_t d = static_cast<uint64_t>(divisor);
+    // Keep x9 unchanged until the quotient is corrected for signed truncation.
+    if((d & (d - 1)) == 0) {
+        unsigned shift = 0;
+        for(uint64_t n = d; n > 1; n >>= 1) ++shift;
+        if(!sqjit_a64_emit_shift_right(buf, 11, 9, 63, true) ||
+            !sqjit_a64_emit_shift_right(buf, 11, 11, 64 - shift, false) ||
+            !sqjit_a64_emit_add_reg(buf, 11, 9, 11) ||
+            !sqjit_a64_emit_shift_right(buf, 11, 11, shift, true)) return false;
+    }
+    else {
+        // Same signed reciprocal generator as the x64 backend. Unsigned
+        // quotient/remainder doubling avoids host overflow and FP rounding.
+        const uint64_t half = UINT64_C(1) << 63;
+        const uint64_t boundary = half - 1 - half % d;
+        uint64_t q[2] = {half / boundary, half / d};
+        uint64_t r[2] = {half % boundary, half % d};
+        const uint64_t denominators[2] = {boundary, d};
+        unsigned power = 63;
+        do {
+            ++power;
+            for(unsigned i = 0; i != 2; ++i) {
+                q[i] <<= 1;
+                r[i] <<= 1;
+                if(r[i] >= denominators[i]) { r[i] -= denominators[i]; ++q[i]; }
+            }
+        } while(q[0] < d - r[1] || (q[0] == d - r[1] && r[0] == 0));
+        const uint64_t magic = q[1] + 1;
+        SQInteger bits;
+        memcpy(&bits, &magic, sizeof(bits));
+        if(!sqjit_a64_emit_mov_imm_x(buf, 10, bits) ||
+            !sqjit_a64_emit_smulh_reg(buf, 11, 9, 10)) return false;
+        if((magic & half) && !sqjit_a64_emit_add_reg(buf, 11, 11, 9)) return false;
+        if(power > 64 && !sqjit_a64_emit_shift_right(buf, 11, 11, power - 64, true)) return false;
+        if(!sqjit_a64_emit_shift_right(buf, 12, 11, 63, false) ||
+            !sqjit_a64_emit_add_reg(buf, 11, 11, 12)) return false;
+    }
+    if(!remainder) return sqjit_a64_emit_mov_reg(buf, 9, 11);
+    return sqjit_a64_emit_mov_imm_x(buf, 10, divisor) &&
+        sqjit_a64_emit_msub_reg(buf, 9, 11, 10, 9);
 }
 
 bool sqjit_a64_emit_msub_reg(SQJitA64Buffer *buf, unsigned rd, unsigned rn, unsigned rm, unsigned ra)
--- a/Squirrel3/squirrel/jit/sqjit_backend_aarch64_emit.h
+++ b/Squirrel3/squirrel/jit/sqjit_backend_aarch64_emit.h
@@ -51,6 +51,11 @@
 bool sqjit_a64_emit_and_reg(SQJitA64Buffer *buf, unsigned rd, unsigned rn, unsigned rm);
 bool sqjit_a64_emit_mul_reg(SQJitA64Buffer *buf, unsigned rd, unsigned rn, unsigned rm);
 bool sqjit_a64_emit_sdiv_reg(SQJitA64Buffer *buf, unsigned rd, unsigned rn, unsigned rm);
+bool sqjit_a64_emit_smulh_reg(SQJitA64Buffer *buf, unsigned rd, unsigned rn, unsigned rm);
+bool sqjit_a64_emit_shift_right(SQJitA64Buffer *buf, unsigned rd, unsigned rn,
+    unsigned shift, bool arithmetic);
+// Positive int64 divisor only. Dividend/result x9; clobbers x10-x12.
+bool sqjit_a64_emit_divmod_constant(SQJitA64Buffer *buf, SQInteger divisor, bool remainder);
 bool sqjit_a64_emit_msub_reg(SQJitA64Buffer *buf, unsigned rd, unsigned rn, unsigned rm, unsigned ra);
 bool sqjit_a64_emit_blr_x16(SQJitA64Buffer *buf);
 bool sqjit_a64_emit_scvtf_float_x(SQJitA64Buffer *buf, unsigned sd, unsigned rn);
--- a/Squirrel3/squirrel/jit/sqjit_backend_aarch64_compile_proto.inc
+++ b/Squirrel3/squirrel/jit/sqjit_backend_aarch64_compile_proto.inc
@@ -518,10 +518,19 @@
     auto emit_binary = [&](SQOpcode op, SQInteger dst, SQInteger left, SQInteger right) -> bool {
         SQInteger guard_patch = 0;
         if(dst < 0 || dst >= MAX_FUNC_STACKSIZE || !ensure_int_slot(left) || !ensure_int_slot(right) ||
-            !sqjit_a64_emit_ldr_x(&buf, 9, 31, sqjit_a64_local_disp(left)) ||
-            !sqjit_a64_emit_ldr_x(&buf, 10, 31, sqjit_a64_local_disp(right))) {
-            return false;
-        }
+            !sqjit_a64_emit_ldr_x(&buf, 9, 31, sqjit_a64_local_disp(left))) {
+            return false;
+        }
+        // Only a proven bytecode constant can remove the divisor guard. Sampled
+        // parameter values and constants invalidated at a CFG join never qualify.
+        if(sizeof(SQInteger) == 8 && (op == _OP_DIV || op == _OP_MOD) &&
+            slots[right].known_const && slots[right].const_value > 0) {
+            if(!sqjit_a64_emit_divmod_constant(&buf, slots[right].const_value, op == _OP_MOD) ||
+                !sqjit_a64_emit_str_x(&buf, 9, 31, sqjit_a64_local_disp(dst)) ||
+                !mark_int_slot(dst)) return false;
+            return true;
+        }
+        if(!sqjit_a64_emit_ldr_x(&buf, 10, 31, sqjit_a64_local_disp(right))) return false;
         switch(op) {
             case _OP_ADD:
                 if(!sqjit_a64_emit_add_reg(&buf, 9, 9, 10)) {
--- a/Squirrel3/squirrel/jit/sqjit_backend_aarch64_compile_loop.inc
+++ b/Squirrel3/squirrel/jit/sqjit_backend_aarch64_compile_loop.inc
@@ -1080,10 +1080,21 @@
     auto emit_binary = [&](SQOpcode op, SQInteger dst, SQInteger left, SQInteger right) -> bool {
         SQInteger guard_patch = 0;
         if(dst < 0 || dst >= MAX_FUNC_STACKSIZE || !ensure_int_slot(left) || !ensure_int_slot(right) ||
-            !sqjit_a64_emit_ldr_x(&buf, 9, 31, sqjit_a64_local_disp(left)) ||
-            !sqjit_a64_emit_ldr_x(&buf, 10, 31, sqjit_a64_local_disp(right))) {
-            return false;
-        }
+            !sqjit_a64_emit_ldr_x(&buf, 9, 31, sqjit_a64_local_disp(left))) {
+            return false;
+        }
+        // Only a proven bytecode constant can remove the divisor guard. Sampled
+        // parameter values and constants invalidated at a CFG join never qualify.
+        if(sizeof(SQInteger) == 8 && (op == _OP_DIV || op == _OP_MOD) &&
+            slots[right].known_const && slots[right].const_value > 0) {
+            bool entry_loaded = slots[left].entry_loaded || slots[right].entry_loaded;
+            if(!sqjit_a64_emit_divmod_constant(&buf, slots[right].const_value, op == _OP_MOD) ||
+                !sqjit_a64_emit_str_x(&buf, 9, 31, sqjit_a64_local_disp(dst)) ||
+                !mark_int_slot(dst)) return false;
+            slots[dst].entry_loaded = entry_loaded;
+            return true;
+        }
+        if(!sqjit_a64_emit_ldr_x(&buf, 10, 31, sqjit_a64_local_disp(right))) return false;
         switch(op) {
             case _OP_ADD:
                 if(!sqjit_a64_emit_add_reg(&buf, 9, 9, 10)) {
--- a/test/cpp/test_sqjit_aarch64_emit.cpp
+++ b/test/cpp/test_sqjit_aarch64_emit.cpp
@@ -72,6 +72,22 @@
         "and rejects invalid registers without emitting code");
 
     SQJitA64Buffer fp_buf = {};
+    SQJitA64Buffer division_buf = {};
+    CHECK(sqjit_a64_emit_smulh_reg(&division_buf, 9, 10, 11) &&
+        sqjit_a64_emit_shift_right(&division_buf, 11, 9, 63, true) &&
+        sqjit_a64_emit_shift_right(&division_buf, 12, 11, 17, false),
+        "signed multiply high and immediate shifts emit");
+    CHECK(word_at(division_buf, 0) == 0x9B4B7D49u &&
+        word_at(division_buf, 1) == 0x937FFD2Bu &&
+        word_at(division_buf, 2) == 0xD351FD6Cu,
+        "constant division primitive encodings");
+    CHECK(!sqjit_a64_emit_smulh_reg(&division_buf, 32, 1, 2) &&
+        !sqjit_a64_emit_smulh_reg(&division_buf, 1, 32, 2) &&
+        !sqjit_a64_emit_smulh_reg(&division_buf, 1, 2, 32) &&
+        !sqjit_a64_emit_shift_right(&division_buf, 32, 1, 2, true) &&
+        !sqjit_a64_emit_shift_right(&division_buf, 1, 32, 2, false) &&
+        !sqjit_a64_emit_shift_right(&division_buf, 1, 2, 64, true) &&
+        division_buf.size == 12, "invalid division primitives do not emit code");
     CHECK(sqjit_a64_emit_scvtf_float_x(&fp_buf, 0, 9), "scvtf emits");
     CHECK(sqjit_a64_emit_fop_float(&fp_buf, _OP_ADD, 1, 2, 3), "fadd emits");
     CHECK(sqjit_a64_emit_fop_float(&fp_buf, _OP_SUB, 4, 5, 6), "fsub emits");
--- a/test/cpp/test_sqjit_arithmetic.cpp
+++ b/test/cpp/test_sqjit_arithmetic.cpp
@@ -1,5 +1,6 @@
 #include "sqpcheader.h"
 #include "jit/sqjit_backend_x64_emit.h"
+#include "jit/sqjit_backend_aarch64_emit.h"
 #include "jit/sqjit_code.h"
 #include "jit/sqjit_platform.h"
 #include <stdint.h>
@@ -56,6 +57,8 @@
             puts("FAIL: floating cache at control-flow joins"); return 1;
         }
     }
+#endif
+#if SQJIT_HAS_X64_NATIVE || SQJIT_HAS_EXTERNAL_NATIVE
     std::vector<SQInteger> divisors = {1,2,3,4,5,7,8,11,17,31,63,64,65,97,127,251,997,
         1000003,1000000007,2147483647,2147483648LL,4294967297LL,
         4611686018427387904LL,9223372036854775807LL};
@@ -63,10 +66,18 @@
     auto next = [&]() { random ^= random << 13; random ^= random >> 7; random ^= random << 17; return random; };
     for(int i=0; i<150; ++i) divisors.push_back((next() >> 1) | 1);
     for(SQInteger d : divisors) for(int remainder=0; remainder<2; ++remainder) {
+#if SQJIT_HAS_X64_NATIVE
         SQJitNativeCodeBuffer buf;
         if(!sqjit_native_emit_mov_rax_arg3(&buf) ||
             !sqjit_native_emit_divmod_constant(&buf,d,remainder != 0) ||
             !sqjit_native_emit_ret(&buf)) return 1;
+#else
+        SQJitA64Buffer buf = {};
+        if(!sqjit_a64_emit_mov_reg(&buf,9,2) ||
+            !sqjit_a64_emit_divmod_constant(&buf,d,remainder != 0) ||
+            !sqjit_a64_emit_mov_reg(&buf,0,9) ||
+            !sqjit_a64_emit_u32(&buf,0xD65F03C0u)) return 1;
+#endif
         SQJitCode code;
         if(!code.Install(buf.bytes,buf.size)) return 1;
         typedef SQInteger (*Function)(void *,void *,SQInteger);
@@ -93,9 +104,15 @@
             }
         }
     }
+#if SQJIT_HAS_X64_NATIVE
     SQJitNativeCodeBuffer buf;
     if(sqjit_native_emit_divmod_constant(&buf,0,false) ||
         sqjit_native_emit_divmod_constant(&buf,-1,true) || buf.size) return 1;
+#else
+    SQJitA64Buffer buf = {};
+    if(sqjit_a64_emit_divmod_constant(&buf,0,false) ||
+        sqjit_a64_emit_divmod_constant(&buf,-1,true) || buf.size) return 1;
+#endif
     puts("[OK] emitted int64 division/remainder: boundaries, multiples and random inputs");
 #endif
     return 0;
--- a/test/cpp/test_sqjit_leaf.cpp
+++ b/test/cpp/test_sqjit_leaf.cpp
@@ -74,6 +74,12 @@
             "function early_return(x) { if(x<0) return -x; if(x==17) return x+1; return x-2 }\n"
             "function call_early_return(x) { return early_return(x) }\n"
             "function division(x,y) { return x/y }\n"
+            "function literal_division(x) { return x/7 }\n"
+            "function literal_remainder(x) { return x%7 }\n"
+            "function power_division(x) { return x/64 }\n"
+            "function power_remainder(x) { return x%64 }\n"
+            "function joined_divisor(x,flag) { local d=7; if(flag) d=11; return x/d }\n"
+            "function changing_divisor(x,n) { local d=7,r=0; for(local i=0;i<n;i++) { r+=x/d; d=11; } return r }\n"
             "function logged_division(a,x,y) { a[0]=a[0]+1; return division(x,y) }\n"
             "function logged_step(a,x,y) { a[0]=a[0]+1; return step(x,y) }\n"
             "function prep_mutation(x) { return step((this.step=9),x) }\n"
@@ -104,6 +110,11 @@
             " for(local x=-30;x<30;x++) { local expected=x<0 ? -x : (x==17 ? x+1 : x-2);\n"
             "  if(call_early_return(x)!=expected) throw \"early return branches\"; }\n"
             " local a=[0]; if(logged_division(a,9,2)!=4 || a[0]!=1) throw \"division value\";\n"
+            " foreach(x in [-1000003,-998,-64,-8,-7,-1,0,1,7,8,64,998,1000003]) {\n"
+            "  if(literal_division(x)!=division(x,7) || literal_remainder(x)!=x-division(x,7)*7 ||\n"
+            "     power_division(x)!=division(x,64) || power_remainder(x)!=x-division(x,64)*64) throw \"signed constant division\";\n"
+            "  if(joined_divisor(x,false)!=division(x,7) || joined_divisor(x,true)!=division(x,11) ||\n"
+            "     changing_divisor(x,9)!=division(x,7)+8*division(x,11)) throw \"divisor changes across control flow\"; }\n"
             " a=[0]; if(logged_step(a,4,2)!=37 || a[0]!=1) throw \"logged call value\";\n"
             " local expected=0; for(local i=0;i<20;i++) expected+=(i*33+1)%97;\n"
             " if(call_loop(20)!=expected || volatile_caller(7)!=12) throw \"loop call value\";\n"
@@ -149,6 +160,13 @@
                 "native loop contains leaf call");
         }
 #endif
+#if SQJIT_HAS_EXTERNAL_NATIVE
+        if(enabled) for(const SQChar *name : {_SC("literal_division"), _SC("literal_remainder"),
+            _SC("power_division"), _SC("power_remainder")}) {
+            CHECK(observe(v, name).frame.successes > 0,
+                "signed constant division and remainder execute native whole-function code");
+        }
+#endif
         CHECK(run(v, _SC(
             "for(local n=0;n<3;n++) { local original=step; local expected=(9*33+2)%97;\n"
             " if(prep_mutation(2)!=expected || step!=9) throw \"prepared callee survives argument mutation\"; step=original; }\n"
