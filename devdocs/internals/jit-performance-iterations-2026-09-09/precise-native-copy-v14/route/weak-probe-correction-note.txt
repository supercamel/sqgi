The original Squirrel sources array dereferenced its weak elements during GET before passing them to MOVE. Its helper-bypass counts remain valid for those actual inputs but do not prove weak-object copying. C++ tests passed weak objects directly and remain authoritative. The replacement Squirrel probe keeps live/dead weak objects in local variables, asserts the returned weakref type, and records V13 MOVE source tags. Run the replacement only after the main timing/profile job terminates.
