The initial V13 expectation counted 30 explicit key loads/copies. The checked run found two further LOADINT instructions at ip1/destination5 overwriting a live OT_TABLE with three references in reused call-frame storage. Typed operand inspection records both; the corrected positive control requires all 32 calls and those two cases. The failed prediction is preserved, not used as bypass evidence.
