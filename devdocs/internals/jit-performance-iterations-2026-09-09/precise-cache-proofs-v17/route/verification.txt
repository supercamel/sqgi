V16/V17 each make 14 SET helper calls and 20 successful native returns, no guard failures or side exits. Loop stores keep their hit paths; mutable owner/key stores retain their guards. The original caller layout took a V16 DLOAD exit at route_key IP0 with a sole-owner OT_TABLE destination; temp_reg was integer. The revised helper-count fixture keeps real owners of prior containers. Existing final-release C++ witnesses remain unchanged and pass. An initial GET-only inspection found no calls and is preserved; inspection of all opcode helpers verifies the DLOAD exit.
