Prepared diagnostic; NOT executed yet. Wait for pipeline session34064to report
terminal before running this GDB/perf job. It captures installed V28 code and
samples in the same inferior PID, with a saved perf map and code bytes. Recording
spans exactly four native buffer calls in the unchanged member suite at n2000000,
warmups0. Only compile/entry/return boundaries have debugger breakpoints; no
instruction or opcode-helper breakpoint is installed inside the body. This is
additional diagnostic evidence and must not replace the accepted timing runs or
seven ordinary profiles. Preserve failures and inspect perf attachment, code
identity, successful returns, lost samples, and sample counts before analysis.
