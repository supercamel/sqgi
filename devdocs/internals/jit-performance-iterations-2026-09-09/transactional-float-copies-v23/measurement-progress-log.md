Historical progress note; superseded by README.md and final validation.

# V23 measurement in progress

V22 remains the last sealed checkpoint (4179 global hashes). V23 is frozen but
not accepted or sealed. Four runtime/test files differ from V22. Binary SHA256:
`a4307d1afa1edf202037e36e4be345d6ccb06143ae65fa1b4c465080d6e89c9f`.

The compiler emits original GP float copies and records their offsets. The
forwarding pass converts only those pairs inside its private transaction, after
validating metadata, exact opcodes, slot bounds and branch leaders. Every refusal
leaves the original GP code untouched. Successful forwarding retains V22 FP copies.

Four 100-test Release/strict-ASan runs and both 41-holdout sets pass. Seven
independent native coverage markers include the new typed-copy/native-bit and
fallback checks. x64/QEMU retains the same 32 known assertions; no x64 pass claimed.
Ten pre/post/installed forwarding captures and the six-entry root/parameter route
checks pass. Filter-stream captures prove exactly ten restored GP instructions and
complete normalized-code equality with V21. The capture harness initially lacked
its symbol manifest and script argument; both failures are retained, then corrected.

The live serial measurement command is `/tmp/sqgi-precise-v23-measure.sh`, log
`/tmp/sqgi-precise-v23-measure.log`, tool session 5016. Never start builds/tests/GDB/
profiles/other timing until that handle is confirmed terminal. It includes full56,
both application seeds/counts, five rotating rounds, warmups4/5, same-candidate
repeats, profitability, seven fresh profiles, precise exits and paired code captures.

After terminal success, run `/tmp/sqgi-precise-v23-post.sh`. The paired decoder now
requires all eight normalized artifacts to equal V22, not merely their guard tails.
Study: `/tmp/sqgi-transactional-float-copies-study`.

Archive/verify/seal scripts are prepared but not run. A V23 report still needs to
be written from actual results, with full regressions/tails, filter controls and
comparison to V22. Retain the native-validation binaries and receiver-publication
witness, verify older hashes before mutable-note updates, then seal and verify.
V23 does not need to duplicate V22's diagnostic preload profile; its seven fresh
ordinary profiles and exact unchanged native-code proof must be retained. Existing
x64 defects, the 2x milestone, final Node/GJS targets and matched PGO/LTO stay open.

Next performance work should use the new profiles. V22 had 45.47% method samples
in VM execution, versus 12.60% owner samples in PINCL helpers and 10.73% in DLOAD.
Register moves dominate the separate float instruction samples. Safe canonical-
frame loop entry is an architectural candidate; never enable the old private-spill
loop takeover for precise functions. No further runtime change is implemented.

The full56 timing stage has completed: 1.7914976891880425x audit, Node/GJS
geometric ratios 8.547046126794747 / 5.651919128784569, p90 24.339757414061864 /
13.629195804195804. No full56 component exceeds 5% slowdown against audit in
this batch. Paired controls and fresh profiles remain pending in live session5016.

Next experiment is now concretely staged outside the frozen source tree at
`/tmp/sqgi-precise-intrinsic-draft`: two compiler files, generated by
`/tmp/sqgi-precise-intrinsic-draft.py`. NOT APPLIED OR BUILT; tests still required.
It adds equality and default-length PREPCALLK/CALL helpers only as a fallback
after the existing strict precise/private compilers fail without a resource error.
Read `/tmp/sqgi-precise-intrinsic-design.md` for proof obligations and tests.
Post-processing additionally captures actual coverage-gap bytecodes/rejections
with `/tmp/sqgi-v23-coverage-probe.py`; use those before choosing implementation.
Do not regenerate prepared V23 scripts blindly: symbol/argument fixes, full-code
paired assertions and metadata-label corrections were added after generation.
