# The bundled Squirrel 3.2 compiler interns an expected-token string during a
# longjmp-based syntax-error path without releasing the final reference. This
# is dependency-owned; all sqgicheck and GI allocations remain unsuppressed.
leak:SQCompiler::Expect
